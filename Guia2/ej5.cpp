#include <vector>

using namespace std;

template <typename T>
void merge(vector<T> &v, int p, int q, int r)
{
    n1 = q - p + 1;
    n2 = r - q;
    vector<T> L(n1);
    vector<T> R(n2);
}

template <typename T>
void mergesort(vector<T> &v)
{
}